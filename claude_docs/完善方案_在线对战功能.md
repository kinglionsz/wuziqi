# 五子棋在线对战功能完善方案

## 一、项目现状分析

### 1.1 技术栈
- **前端**：React 19 + Vite 7 + CSS
- **后端**：无（需要新建）
- **数据库**：Supabase（已有部分表结构）
- **现有功能**：PVP、PVE（AI）、回放、主题、计时、战绩保存

### 1.2 已预留的扩展点
- `GAME_MODES.ONLINE = 'online'` 常量已定义
- Supabase 中已有 `game_rooms` 表的 CRUD 函数（未完成）
- 胜负判定 `checkWinner` 函数已存在于 `src/utils/ai.js`

---

## 二、完善方案（基于方案文档 + 项目现状）

### 2.1 后端架构

**目录结构**：
```
wuziqi/
├── server/                    # 新建后端目录
│   ├── package.json
│   ├── server.js              # 主服务器文件
│   └── utils/
│       └── gameLogic.js       # 游戏逻辑（胜负判定）
├── src/
│   └── ...
```

**技术选型**：
- Node.js + Express
- Socket.io 4.x（ rooms 功能原生支持）
- cors（跨域处理）

### 2.2 数据结构设计

沿用方案文档设计，新增字段：

```javascript
const rooms = {
  "ABCD12": {
    roomId: "ABCD12",
    players: {
      "socketId_A": { role: "black", ready: false, userId: "user_xxx" },
      "socketId_B": { role: "white", ready: false, userId: "user_yyy" }
    },
    board: Array(15).fill(null).map(() => Array(15).fill(null)),
    currentTurn: "black",
    status: "waiting" | "ready" | "playing" | "finished",
    spectators: [],           // 旁观者
    winner: null,             // 获胜者
    startTime: null,          // 游戏开始时间
    moveHistory: []           // 落子历史（用于重连恢复）
  }
}
```

### 2.3 Socket 事件清单

| 事件名 | 方向 | 说明 |
|--------|------|------|
| `create_room` | C→S | 创建房间 |
| `join_room` | C→S | 加入房间 |
| `room_created` | S→C | 创建成功，返回房间号 |
| `join_success` | S→C | 加入成功 |
| `join_error` | S→C | 加入失败 |
| `game_start` | S→C | 游戏开始 |
| `place_piece` | C→S | 落子 |
| `sync_board` | S→C | 同步棋盘 |
| `game_over` | S→C | 游戏结束 |
| `opponent_disconnected` | S→C | 对手掉线 |
| `player_reconnected` | S→C | 玩家重连 |
| `request_undo` | C→S | 申请悔棋 |
| `agree_undo` | C→S | 同意悔棋 |
| `reject_undo` | C→S | 拒绝悔棋 |
| `chat_message` | C→S | 聊天消息 |
| `sync_chat` | S→C | 同步聊天 |

---

## 三、开发计划

### 阶段一：后端基础架构

#### PR 1.1：搭建 Node.js + Socket.io 服务器
- [ ] 创建 `server/` 目录
- [ ] 创建 `server/package.json`（依赖：express, socket.io, cors）
- [ ] 创建 `server/server.js`（基础 WebSocket 连接）
- [ ] 实现房间创建/加入逻辑
- [ ] 更新 `package.json` 添加 `server` 启动脚本

#### PR 1.2：实现落子同步与胜负判定
- [ ] 创建 `server/utils/gameLogic.js`（移植前端 `checkWinner`）
- [ ] 实现 `place_piece` 事件处理
- [ ] 实现后端胜负判定
- [ ] 实现棋盘状态广播

### 阶段二：前端在线对战功能

#### PR 2.1：前端 Socket 连接与房间 UI
- [ ] 安装 `socket.io-client` 依赖
- [ ] 创建 `src/hooks/useOnlineGame.js`（在线游戏 Hook）
- [ ] 修改 `src/utils/constants.js`（添加 Socket 配置）
- [ ] 在 `App.jsx` 添加"在线对战"模式入口
- [ ] 创建房间 UI 组件
- [ ] 创建加入房间 UI 组件

#### PR 2.2：在线对战游戏逻辑
- [ ] 实现 `useOnlineGame` Hook 的核心逻辑
- [ ] 连接 Socket 事件
- [ ] 实现房间状态管理
- [ ] 实现断线重连提示 UI

### 阶段三：进阶功能

#### PR 3.1：断线重连与掉线处理
- [ ] 实现基于 userId 的重连机制
- [ ] 添加掉线倒计时 UI
- [ ] 实现超时判负逻辑

#### PR 3.2：附加功能
- [ ] 悔棋功能（request_undo / agree_undo）
- [ ] 聊天功能
- [ ] 旁观者模式

---

## 四、关键文件修改清单

### 新增文件
| 文件路径 | 说明 |
|---------|------|
| `server/package.json` | 后端依赖配置 |
| `server/server.js` | 主服务器 |
| `server/utils/gameLogic.js` | 游戏逻辑 |
| `src/hooks/useOnlineGame.js` | 在线游戏 Hook |
| `src/components/Online/` | 在线对战组件目录 |
| `src/components/Online/RoomModal.jsx` | 房间创建/加入弹窗 |
| `src/components/Online/OnlineBoard.jsx` | 在线对战棋盘 |

### 修改文件
| 文件路径 | 修改内容 |
|---------|---------|
| `package.json` | 添加 socket.io-client 依赖，添加 server 脚本 |
| `src/App.jsx` | 添加在线对战模式入口 |
| `src/utils/constants.js` | 添加在线配置常量 |
| `src/hooks/useGameLogic.js` | 整合在线模式支持 |

---

## 五、注意事项

1. **防作弊**：所有落子校验必须在后端进行
2. **状态一致性**：使用后端作为唯一数据源
3. **用户体验**：落子后立即锁定棋盘，防止重复提交
4. **断线处理**：30秒超时后判负
5. **端口规划**：
   - 前端：3000（开发）/ 5173（Vite）
   - 后端：3001
